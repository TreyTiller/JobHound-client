import React from 'react';


class UpdateTracker extends React.Component {

    // I would declare a state here that would hold the job tracker data from the databse

    // Then using componentWillMount I can make a call to the database for the information and use that to populate the form 
    // with the pre-existing data

    //Then allow the user to update any of the input on a handleChange event

    // Make a PUT request containing that data

}

export default UpdateTracker;